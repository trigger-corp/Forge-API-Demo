forge.logging.info("Start your app here!");var API_demo = {	Models:      {},	Templates:   {},	Collections: {},	Views:       {},	Utils:       {},		_loadTemplates: function () {		$('script[type="x-templ-mustache"]').each(function () {			var templateName = this.id.replace(/^template_/, '');			API_demo.Templates[templateName] = this.innerHTML;		});	},	init: function () {		// loads templates from index.html		this._loadTemplates();		//clear the badge numbers		forge.button.setBadge(0);				// starting point of the app		API_demo.router = new API_demo.Router();		Backbone.history.start();	}};